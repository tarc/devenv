# generic-agent.HARNESS.1
# Running through the devenv shell lets devenv-run-tests point the `devenv`
# module input at this checkout instead of the published one. The first check
# in .test.sh asserts that it did.
use_shell: true
use_tmp_dir: false
